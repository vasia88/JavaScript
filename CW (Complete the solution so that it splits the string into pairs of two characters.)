function solution(str){
 let arr = [];
    let symbol = '_';

    if (str.length % 2 !== 0) {
        str += symbol;
    }

    for (let i = 0; i < str.length; i = i + 2) {
        arr.push(str[i] + str[i + 1]);
    }
    console.log(arr);
    return arr;
}
